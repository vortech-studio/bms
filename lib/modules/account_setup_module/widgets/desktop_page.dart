import 'package:flutter/material.dart';

class AccountSetupDesktopPage extends StatelessWidget {
  const AccountSetupDesktopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
